<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="libbook.list">
      <field name="name">libbook list</field>
      <field name="model">libbook.libbook</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="libbook.action_window">
      <field name="name">libbook window</field>
      <field name="res_model">libbook.libbook</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="libbook.action_server">
      <field name="name">libbook server</field>
      <field name="model_id" ref="model_libbook_libbook"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="libbook" id="libbook.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="libbook.menu_1" parent="libbook.menu_root"/>
    <menuitem name="Menu 2" id="libbook.menu_2" parent="libbook.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="libbook.menu_1_list" parent="libbook.menu_1"
              action="libbook.action_window"/>
    <menuitem name="Server to list" id="libbook" parent="libbook.menu_2"
              action="libbook.action_server"/>
    -->
  </data>
</odoo>